spring:
  application:
    name: bored-service-consumer-login
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

feign:
  sentinel:
    enabled: true

server:
  port: 8601

management:
  endpoints:
    web:
      exposure:
        include: "*"
