spring:
  application:
    name: bored-service-consumer-space
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

#feign:
#  sentinel:
#    enabled: true

server:
  port: 8602

management:
  endpoints:
    web:
      exposure:
        include: "*"
