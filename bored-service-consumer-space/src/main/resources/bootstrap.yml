spring:
  application:
    name: bored-service-consumer-space
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

feign:
  sentinel:
    enabled: true

server:
  port: 9081

management:
  endpoints:
    web:
      exposure:
        include: "*"
