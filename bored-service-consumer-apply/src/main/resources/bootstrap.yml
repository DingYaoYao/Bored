spring:
  application:
    name: bored-service-consumer-apply
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
feign:
  sentinel:
    enabled: true


server:
  port: 9025

management:
  endpoints:
    web:
      exposure:
        include: "*"
