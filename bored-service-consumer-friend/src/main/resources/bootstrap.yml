spring:
  application:
    name: bored-service-consumer-friend
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
feign:
  sentinel:
    enabled: true

server:
  port: 9061

management:
  endpoints:
    web:
      exposure:
        include: "*"
