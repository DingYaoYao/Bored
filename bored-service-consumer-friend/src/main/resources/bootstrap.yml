spring:
  application:
    name: bored-service-consumer-friend
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848


server:
  port: 9041

management:
  endpoints:
    web:
      exposure:
        include: "*"
