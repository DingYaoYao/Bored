spring:
  application:
    name: bored-service-consumer-reg
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848


server:
  port: 8801

management:
  endpoints:
    web:
      exposure:
        include: "*"
